#include <stdio.h>
#include <string.h>

int main() {
    char msg[64] = "Joke's on you, there is no system!\n";
    char buf[64];
    write (1,&msg,strlen(msg));
    read(0,buf,256);
    return 0;
    }
